# Unknown status fixture

**Status:** in progress

## Steps

1. Synthetic step. `in progress` is not in the plan vocabulary, so the lifecycle checks cannot
   classify this task — which is why it needs a check of its own rather than silent exemption.
